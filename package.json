{
  "name": "vocab-quiz",
  "private": true,
  "version": "1.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:deploy": "vite build && node -e \"require('fs').copyFileSync('sw.js','dist/sw.js')\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.14.0",
    "localtunnel": "^2.0.2",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.4.0"
  }
}
